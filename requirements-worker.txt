# Full dependency set for running the actual six-stage pipeline: the
# GitHub Actions worker (worker.py, see .github/workflows/process_document.yml)
# installs this, and so should you locally if you want to run the whole
# pipeline yourself (PIPELINE_EXECUTOR=thread, the default for local dev).
#
# The always-on web app (main.py) does NOT need this file — see the slim
# requirements.txt instead, which is what actually gets deployed for the
# web process.

# --- Web framework (also needed here since worker.py imports config.py,
#     which is shared with main.py; no server actually runs in the worker) ---
fastapi==0.141.1
python-multipart==0.0.20
jinja2==3.1.6

# --- Data validation ---
pydantic==2.12.5
python-dotenv==1.2.3

# --- Document processing (Stage 1: Document Reader) ---
pymupdf==1.28.2
pytesseract==0.3.13
pillow==11.3.0

# --- Agent orchestration (Stages 3, 4, 5: Subject Interpreter, Explanation
#     Agent, Study-Pack Agent — the actual multi-agent CrewAI Crews) ---
crewai==1.15.21

# --- Gemini vision + embeddings (Stage 4, Stage 6) ---
google-genai==2.23.0

# --- Text-to-speech chapter audio (Stage 5) ---
edge-tts==7.2.8

# --- Vector database client (Stage 6) ---
qdrant-client==1.19.0

# --- Persistence ---
supabase==2.31.0

# --- Retry logic (Stage 4/5/6 LLM calls) ---
tenacity==9.1.2

# --- Testing ---
pytest==8.4.2
